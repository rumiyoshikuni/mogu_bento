<div class="container px-5 px-sm-0">
  <div calss="row">
    <div class="col-xs-12">
      <h5 class="alert alert-secondary text-center">ジャンル編集</h5>
        <%= form_with model: @genre, url: admin_genre_path(@genre.id), method: :patch do |f| %>
          <div>
            ジャンル名
            <%= f.text_field :name  %>
            <%= f.submit "変更を保存", class: "btn btn-success" %>
          </div>
        <% end %>
    </div>
  </div>
</div>