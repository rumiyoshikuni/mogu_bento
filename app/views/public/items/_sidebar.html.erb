<div>
  <table class="table table-hover">
    <thead><p class="text-center mt-5">ジャンル検索</p></thead>
    <tbody>
      <% genres.each do |genre| %>
        <tr>
          <td>
            <%= link_to items_path(genre_id: genre.id), class: "text-decoration-none text-secondary" do %>
              <%= genre.name %>
            <% end %>
          </td>
        </tr>
    　<% end %>
    </tbody>
  </table>
</div>